{
  "private": true,
  "workspaces": [
    "packages/*",
    "client",
    "server"
  ],
  "scripts": {
    "build:validation": "npm --workspace=@selnic/validation run build"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
